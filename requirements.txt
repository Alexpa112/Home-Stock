Flask==3.0.3
Flask-WTF>=1.2,<2
pytesseract==0.3.13
Pillow>=10.0
requests>=2.28.0
python-dotenv>=1.0.0
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.21.0
opencv-python-headless>=4.9,<=4.12.0.88
gunicorn>=21.2,<22
argostranslate>=1.9,<2; platform_machine != "armv7l"
PyJWT>=2.8,<3
cryptography>=42,<43
# Soporte Postgres opt-in (DB_ENGINE=postgres, ver stockhogar/db_backend.py).
# -binary evita compilar contra libpq en la Raspberry Pi.
psycopg2-binary>=2.9,<3
# Notificaciones push (P-01/P-07, ver stockhogar/servicios/push_service.py).
# Arrastra py-vapid (generacion/firma de claves VAPID) y http-ece (cifrado
# del payload) como dependencias transitivas.
pywebpush>=2.0,<3
