Flask==3.1.3
Flask-WTF>=1.2,<2
# Motor principal del escaner de tickets (Claude Vision). Sin este paquete el
# escaner cae siempre a Tesseract aunque ANTHROPIC_API_KEY este configurada,
# porque claude_ocr.py importa 'anthropic' de forma opcional.
anthropic>=0.69
pytesseract==0.3.13
Pillow>=10.0
requests>=2.28.0
python-dotenv>=1.0.0
rapidfuzz>=3.0,<4
python-Levenshtein>=0.21.0
opencv-python-headless>=4.9,<=4.12.0.88
gunicorn>=22.0,<23
argostranslate>=1.9,<2; platform_machine != "armv7l"
PyJWT>=2.8,<3
cryptography>=50.0,<51
# Soporte Postgres opt-in (DB_ENGINE=postgres, ver stockhogar/db_backend.py).
# -binary evita compilar contra libpq en la Raspberry Pi.
psycopg2-binary>=2.9,<3
# Notificaciones push (P-01/P-07): OPCIONAL, se instala en produccion si es necesario.
# http-ece (cifrado del payload) requiere compilacion, lo que falla en CI;
# como el codigo ya maneja la ausencia con graceful degradation, se omite de
# la instalacion automatica pero puede instalarse manualmente si se necesita.
# py-vapid y pywebpush son transdependencias de push, comentadas:
# py-vapid>=1.7,<2
# pywebpush>=2.0,<3  # Instalacion optativa en produccion
